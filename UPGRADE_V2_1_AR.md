# الترقية الآمنة من V2.0 إلى V2.1 على Termux

لا تحذف مجلد V2.0 قبل نجاح V2.1. أهم بياناتك هي `.env` و`data/whatsapp_ops.db` و`data/wa_provider/`.

1. أوقف V2.0 وخذ نسخة كاملة من `data` و`.env`.
2. فك V2.1 في مجلد جديد.
3. انسخ `.env` و`data` كاملًا إلى المجلد الجديد.
4. أنشئ `.venv` وثبت `requirements.txt` ثم `npm install` داخل `provider`.
5. شغّل `./botctl.sh selfcheck` ثم `start/status`.

بعد الترقية افتح لوحة التحكم. إذا كان عدد الرسائل كبيرًا لكن `رسائل ذات نص قابل للقراءة` أو `رسائل تحتوي URL` يساوي صفرًا/منخفضًا جدًا، افتح الحساب واضغط `♻️ إعادة مزامنة السجل بالـQR`. هذه الخطوة تعيد ربط ذلك الحساب فقط حتى يرسل WhatsApp السجل المتاح للقارئ الجديد. مقدار التاريخ الذي يرسله WhatsApp يظل خاضعًا لعميل Multi-Device.
