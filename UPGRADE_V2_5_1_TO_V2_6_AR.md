# ترقية V2.5/V2.5.1 إلى V2.6

V2.6 يستخدم migrations إضافية ويحافظ على بيانات V2.5/V2.5.1. قبل الترقية خذ نسخة من `.env` ومجلد `data`.

بعد فك V2.6:

1. انسخ `.env` القديم إلى مجلد V2.6.
2. انسخ `data` القديم إلى مجلد V2.6.
3. أنشئ `.venv` جديدًا وثبت `requirements.txt`.
4. نفذ `npm install --omit=dev` داخل `provider`.
5. نفذ `./botctl.sh selfcheck`.
6. شغل `./botctl.sh start`.

عند أول تشغيل ينشئ V2.6 الجداول الجديدة ويضيف أعمدة المهام الجديدة بدون حذف جداول V2.5.
