# إصلاحات V2.5.1

- إصلاح ترقية جدول `telegram_sources` عند نقل قاعدة بيانات V2.4 إلى V2.5 بدون حذف المصادر القديمة.
- الاحتفاظ بجدول V2.4 القديم باسم `telegram_sources_v24_legacy` كنسخة أمان داخل قاعدة البيانات.
- ترحيل أقسام روابط Telegram القديمة إلى `link_sections` عندما تكون البيانات متاحة.
- إظهار خطأ واضح داخل Telegram إذا تعذر فتح صفحة المصادر بدل زر بلا استجابة.
- شاشة إرسال الرسائل إلى الدردشات الحالية تعرض الآن معاينة مرقمة لأول المستلمين مع الاسم المحفوظ والرقم/JID.
- شاشة اختيار المستلمين تعرض الرقم التسلسلي والاسم والرقم عبر صفحات.
- إصلاح `botctl.sh selfcheck` ليبحث عن Baileys داخل مجلد `provider` الصحيح.
