# الدليل الكامل للأزرار والوظائف
## WhatsApp Link Ops Bot V2.6 FULL MERGED FINAL

هذا الدليل يشرح أزرار الواجهة ووظائف البوت كما هي مطبقة في الكود. الأزرار التي تحمل رقم حساب أو مهمة أو مصدر أو رسالة تتكرر ديناميكيًا، لذلك يشرح الدليل **نوع الزر** مرة واحدة مع طريقة استخدامه.

---

# 1) فكرة البوت

البوت عبارة عن لوحة إدارة عبر Telegram لمهام WhatsApp مرتبطة بموصل محلي يعتمد WhatsApp Web Multi-Device عبر Baileys. يحتفظ البوت بقاعدة SQLite موحدة للروابط والمهام والتقارير، بينما يحتفظ كل حساب WhatsApp بجلسة مستقلة.

أهم مجموعات الوظائف:

- ربط وإدارة عدة حسابات WhatsApp عبر QR.
- مزامنة الرسائل التي يصل بها سجل WhatsApp إلى الموصل.
- استخراج الروابط وتصنيفها ومنع التكرار عالميًا.
- مصادر Telegram وجلب روابط جديدة، مع استيراد التاريخ عبر Telethon اختياريًا.
- فحص روابط مجموعات WhatsApp العامة.
- طابور انضمام منظم حسب القسم والحساب والعدد والدفعات.
- إدارة قوالب الرسائل والحملات للمجموعات أو المحادثات الموجودة أصلًا.
- مهام خلفية قابلة للإيقاف والإلغاء والاستكمال.
- جدولة تذكيرات ونسخ احتياطية وفحوصات إدارية.
- Inbox للمتابعة، Notes، Tags، وتذكيرات للمحادثات.
- البحث الشامل والتقارير والنسخ الاحتياطي والتشخيص.

> البوت لا يحاول تجاوز قيود WhatsApp. عند ظهور Rate Limit أو Try Later تتوقف العملية أو تحفظ حالتها للاستكمال لاحقًا.

---

# 2) مستويات الصلاحية

## المالك Owner

هو `OWNER_ID` في `.env`، ويملك كل الوظائف بما فيها إدارة المشرفين، التصدير الكامل، إعادة الضبط الكاملة، والإدارة العامة.

## المشرف الرئيسي Principal

يمتلك تقريبًا كل وظائف التشغيل والإدارة، لكنه لا يستطيع إدارة قائمة المشرفين ولا تنفيذ إعادة ضبط كاملة تمس صلاحيات المالك.

## المشرف العادي Registry Supervisor

تظهر له واجهة مصغرة فقط:

- `📱 حساباتي WhatsApp`
- `⛔ سجل الروابط المنتهية`
- `🗑 سجل الروابط المهمشة`

ويتعامل مع حساباته الخاصة.

---

# 3) الشاشة الرئيسية

## `📱 الحسابات`

يفتح إدارة حسابات WhatsApp المرتبطة.

تستخدمه عندما تريد إضافة حساب، فحص اتصاله، رؤية مجموعاته، إعادة ربطه، تعطيله أو حذفه.

## `🚀 لوحة التحكم`

يعرض ملخصًا حيًا عن النظام:

- عدد الحسابات.
- عدد الحسابات المتصلة.
- عدد رسائل WhatsApp المتزامنة محليًا.
- الرسائل التي تحتوي نصًا.
- الرسائل التي تحتوي URL.
- عدد مهام التشغيل النشطة.
- عدد المهام المجدولة.
- عدد المحادثات التي تحتاج متابعة.
- عدد الأخطاء المفتوحة.
- إجمالي الروابط والمنتهية والمهمشة.

ومن داخله توجد اختصارات إلى صحة الحسابات والمتابعات والمهام المجدولة والأخطاء والتقرير الشامل.

## `🔗 الروابط والتجميع`

يفتح مركز جمع وإدارة الروابط.

## `🔍 الفحص والانضمام`

يفتح أدوات فحص روابط WhatsApp وطابور الانضمام.

## `📨 إدارة الرسائل`

يفتح Inbox والقوالب والإرسال والحملات ودليل WhatsApp وقائمة عدم الإرسال.

## `📋 المهام والتقارير`

يفتح مهام التشغيل والجدولة وصحة الحسابات والتقارير والحملات.

## `👁 المراقبة المباشرة`

يتيح مراقبة مجموعة WhatsApp محددة وجمع الروابط من الرسائل الجديدة فقط.

## `⚙️ الإدارة والنظام`

خاص بالمالك/المشرف الرئيسي. يحتوي النسخ الاحتياطية والتشخيص والأخطاء وسجل الإدارة وفحص القاعدة وإعادة الضبط.

## `⬅️ الرئيسية`

يرجع إلى القائمة الرئيسية ويلغي حالة الإدخال الحالية عند الرجوع من بعض المسارات.

## `❌ إلغاء العملية`

يلغي خطوة الإدخال الحالية بدون تنفيذ التغيير الجاري.

---

# 4) قسم الحسابات

## `➕ ربط حساب جديد QR`

1. اضغط الزر.
2. أرسل اسمًا للحساب مثل `حساب اليمن 1`.
3. ينشئ البوت Slot وحساب Provider مستقلًا.
4. اضغط `🔳 عرض QR`.
5. من WhatsApp افتح الأجهزة المرتبطة ثم اربط الجهاز.

## زر الحساب `📱 #رقم الاسم`

يفتح صفحة حساب محدد. يظهر اسم الحساب، حالته، وآخر خطأ.

## `🔳 عرض QR/إعادة الربط`

إذا كان الحساب غير متصل يعرض QR لربطه. إذا كان متصلًا يخبرك أنه متصل بالفعل.

لا ترسل QR لأي شخص لأنه يسمح بربط جلسة WhatsApp.

## `🧪 اختبار`

يسأل Provider عن حالة الحساب الحالية ويحدث `health` و`last_error` ورقم الحساب/المعرف إن توفر.

## `👥 عرض المجموعات`

يعرض حتى أول 100 مجموعة متاحة للحساب، ويتضمن:

- اسم المجموعة.
- JID.
- عدد الأعضاء إذا توفر.

يمكن استخدام JID لاحقًا في المراقبة أو قائمة عدم الإرسال.

## `⏯ تفعيل/إيقاف`

يعطل أو يفعل استخدام الحساب داخل البوت بدون حذف جلسته.

## `♻️ إعادة مزامنة السجل بالـQR`

ميزة صيانة قوية. عند تأكيدها:

- يلغي ربط جلسة الجهاز.
- يحذف النسخة المحلية من رسائل هذا الحساب فقط.
- يحذف مؤشرات المزامنة والتجميع لهذا الحساب فقط.
- لا يحذف قاعدة الروابط العالمية.
- ثم يعيد تشغيل Provider ويطلب ربط QR من جديد.

تستخدمها إذا كانت الرسائل موجودة لكن النصوص فارغة أو إذا أردت إعادة بناء السجل المحلي.

## `✅ نعم، إعادة المزامنة`

ينفذ عملية إعادة المزامنة السابقة بعد التحذير.

## `🗑 حذف وربط الجهاز`

يسجل خروج جلسة WhatsApp ويحذف Slot الحساب من قاعدة البوت.

استخدمه فقط إذا لم تعد تريد الحساب داخل النظام.

## `🔄 تحديث الحالة`

يظهر بعد إنشاء حساب جديد ويقوم بنفس فحص الاتصال الأساسي.

---

# 5) لوحة التحكم V2.6

## `🩺 صحة الحسابات`

يفحص كل الحسابات المفعلة ويحسب Health Score تقريبيًا من 0 إلى 100.

أمثلة:

- متصل: قريب من 100%.
- Connecting: أقل.
- QR/غير مربوط: أقل.
- Provider Error: 0%.

أي خطأ اتصال مهم يسجل أيضًا في Error Center.

## `📥 المتابعات`

يفتح صندوق متابعة محادثات WhatsApp الشخصية.

## `⏰ المجدولة`

يفتح مركز المهام المجدولة.

## `⚠️ الأخطاء`

يفتح Error Center.

## `📊 تقرير جميع الحسابات`

يعرض تقريرًا يجمع لكل حساب:

- الحالة.
- عدد الرسائل والنصوص.
- المجموعات.
- الدردشات.
- القنوات.
- محاولات الانضمام.
- الحملات وأهداف الإرسال الناجحة.
- آخر خطأ.

إذا كان التقرير أطول من حد Telegram، يرسله كملف نصي.

---

# 6) الروابط والتجميع

## `⚡ تجميع من WhatsApp`

يبدأ Wizard لجمع الروابط من الرسائل المحلية المتزامنة للحسابات.

### اختيار نوع الروابط

#### `WhatsApp — جميع روابط واتساب`

يستخرج روابط WhatsApp المطابقة.

#### `Telegram`

يستخرج روابط Telegram الموجودة داخل رسائل WhatsApp.

#### `X/Twitter`

يستخرج روابط X/Twitter.

#### `كل الروابط`

يستخرج جميع URL التي يدعمها القارئ.

### اختيار المصدر

#### `كل الحسابات/كل المجموعات`

يفحص الرسائل المتزامنة من كل المصادر المتاحة للحسابات ضمن صلاحيات المستخدم.

#### `مجموعة محددة`

يطلب JID أو رابط دعوة لمجموعة. إذا أرسلت رابط دعوة يحاول حلّه إلى JID بواسطة حساب متصل.

### طريقة القراءة

#### `⚡ سريع — رسائل تحتوي روابط فقط`

مصمم لتقليل العمل والتركيز على الرسائل المتوقع أن تحتوي روابط.

#### `🔎 عميق — فحص كل النصوص المتزامنة`

يفحص كل النصوص المتزامنة ضمن النطاق المحدد ويبحث داخلها عن الروابط.

### الفترة

#### `🆕 الجديد فقط`

يستخدم Cursor ويقرأ ما لم يعالج سابقًا.

#### `الكل المتزامن`

يفحص كامل السجل المحلي المتزامن المتاح.

#### `30 يوم / 14 يوم / 7 أيام / 3 أيام / 24 ساعة`

يقيد القراءة زمنيًا بالفترة المحددة.

بعد الاختيار ينشئ Job في الخلفية ويظهر تقريرًا يتضمن الرسائل المقروءة والنصوص ورسائل URL والروابط الجديدة والمكررة والمحظورة.

## `✍️ استقبال روابط يدويًا`

ألصق روابط في رسالة واحدة أو عدة أسطر. يقوم البوت بالتطبيع والتصنيف ومنع التكرار، ويظهر:

- المكتشف.
- الجديد.
- المكرر.
- المحظور لأنه سبق تسجيله كمنتهي.

## `📡 مصادر Telegram للروابط`

إدارة القنوات/المجموعات التي تجمع منها روابط WhatsApp.

## `🗃 قاعدة الروابط`

تعرض إحصاءات قاعدة الروابط:

- إجمالي الروابط الفريدة.
- المنتهية.
- المهمشة.
- توزيع الأقسام.
- توزيع أنواع الروابط.

### `📦 تصدير ZIP`

للمالك/المخول: يصدر قاعدة الروابط في تقرير ZIP.

## `🔎 البحث`

البحث الشامل V2.6. يقبل:

- رابط.
- كلمة.
- JID.
- اسم مصدر.
- جزءًا من رسالة.
- رقم مهمة.

ويبحث في الروابط، رسائل WhatsApp، المهام، مصادر Telegram، وملاحظات المتابعة.

## `⛔ المنتهية`

يطلب منك لصق روابط WhatsApp المنتهية. عند إدخالها:

- تسجل في `expired_registry`.
- تحذف من طابور الانضمام.
- تزال من الأقسام النشطة.
- تزال من جدول الروابط النشط.
- يمنع إدخالها لاحقًا كروابط فعالة.

## `🗑 المهمشة`

تسجل الروابط التي لا تريد اعتبارها مهمة. يتم استبعادها من الإدخال والفحص والانضمام مستقبلًا.

---

# 7) مصادر Telegram

## `➕ إضافة مصدر Telegram`

يطلب أولًا القسم الذي ستذهب إليه الروابط القادمة من المصدر.

### أقسام المصدر

- `⭐ الروابط المهمة`
- `🎓 روابط الطلبة`
- `⛔ الروابط المنتهية`
- `🗑 الروابط المهمشة`
- `📢 روابط القنوات`

بعد اختيار القسم أرسل:

- `@username`
- أو رابط `https://t.me/...`
- أو Chat ID.

يجب أن يكون البوت قادرًا على الوصول إلى القناة/المجموعة. في المصادر الخاصة غالبًا يحتاج أن يكون مضافًا/مشرفًا.

## زر `⏸ #رقم` أو `▶️ #رقم`

يفعل أو يوقف مصدر Telegram بدون حذفه.

## `🔄 الحالة`

يعرض قسم المصدر، حالته، وعدد الروابط التي جُمعت منه.

## `🕘 استيراد القديم`

يظهر فقط إذا كانت جلسة Telethon مخولة. يبدأ Job لقراءة تاريخ المصدر السابق مع Cursor محفوظ يمكن الاستكمال منه.

## `🗑 حذف المصدر`

يحذف تعريف المصدر من قائمة مصادر البوت.

## `🔐 جلسة Telegram للتاريخ`

يعرض حالة Telethon ويوضح كيفية تسجيل الجلسة محليًا عبر:

`python telegram_session_login.py`

لا يطلب البوت كود Telegram أو 2FA داخل المحادثة.

---

# 8) الفحص

## `🧪 فحص روابط WhatsApp`

يفتح خيارين:

### `فحص روابط قاعدة البيانات`

يجلب روابط `whatsapp_group` من قاعدة الروابط ويبدأ Audit خلفي.

### `لصق روابط للفحص`

تلصق روابط دعوات WhatsApp ويبدأ Audit لها فقط.

الفحص يستخدم صفحات الدعوة العامة عندما يمكن ذلك، ولا يحتاج استخدام حساب WhatsApp لكل رابط قابل للتصنيف من الويب.

بعد انتهاء الفحص تظهر أزرار:

## `➕ إضافة المجموعات الصالحة لطابور الانضمام`

يأخذ نتائج `group_active` ويضيفها إلى قسم المهمة وطابور الانضمام، مع منع التكرار.

## `📦 تصدير التقرير`

يصدر Audit محدد في ZIP.

---

# 9) إدارة طابور الانضمام

## `➕ إدارة طابور الانضمام`

يعرض الأقسام وأعداد روابط الطابور.

### `⭐ المهمة (عدد)`

اختيار قسم الروابط المهمة.

### `🎓 الطلبة (عدد)`

اختيار قسم الطلبة.

### `📢 القنوات (عدد)`

اختيار قسم القنوات، لكن محرك الانضمام نفسه ينفذ فقط على روابط مجموعات WhatsApp داخل هذا القسم؛ روابط قنوات WhatsApp تبقى محفوظة ولا تمر إلى محرك انضمام المجموعات.

### `⛔ المنتهية — مستبعدة`

زر معلومات فقط؛ هذه الروابط لا تدخل الانضمام.

### `🗑 المهمشة — مستبعدة`

زر معلومات فقط؛ هذه الروابط لا تدخل الانضمام.

### `➕ إضافة روابط يدويًا للمهمة`

ألصق روابط مجموعات WhatsApp، فتُضاف إلى قسم المهمة وإلى Queue إذا لم تكن مكررة/محظورة.

### اختيار الحساب

بعد اختيار القسم تظهر الحسابات المتصلة:

- `📱 #رقم اسم الحساب`
- `🌐 جميع الحسابات المتصلة`

اختيار جميع الحسابات يجعل المعالج يوزع التنفيذ على الحسابات المتاحة وفق منطق Worker.

### اختيار العدد

- `▶️ كل روابط القسم`
- `200`
- `500`
- `1000`
- `🔢 عدد آخر`

ثم يطلب البوت:

1. الفاصل بين كل محاولة والتي بعدها بالثواني.
2. حجم الدفعة.
3. مدة الراحة بين الدفعات بالدقائق.

بعدها ينشئ Job يمكن متابعته وإيقافه واستكماله.

## `🔄 تحديث طلبات 48 ساعة`

يعيد فحص/تحديث حالات الانضمام المعلقة وفق منطق Worker الخاص بالطلبات السابقة.

## `📦 تقرير الانضمام`

يصدر تقرير ZIP لحالات ومحاولات الانضمام حسب الحساب.

---

# 10) إدارة الرسائل

## `📥 صندوق المتابعة`

يعرض أحدث 20 محادثة شخصية متزامنة لكل حساب مسموح.

الحالات المرئية:

- 🔵 جديد.
- 🟡 متابعة.
- ⭐ مهم.
- ✅ مكتمل.
- ⚪ تجاهل.

زر `💬 #رقم JID` يفتح تفاصيل المحادثة.

### داخل المحادثة

#### `⭐ مهم`

يغير حالة المحادثة إلى Important.

#### `🟡 متابعة`

يضعها في Follow-up.

#### `✅ مكتمل`

يعلمها كمكتملة.

#### `⚪ تجاهل`

يضعها في Ignored بدون حذف الرسائل.

#### `📝 ملاحظة`

يطلب نصًا ويحفظ Note مرتبطة بالمحادثة.

#### `🏷 Tag`

يطلب Tag مثل `عميل` أو `طالب` أو `متابعة` ويضيفه للمحادثة.

#### `⏰ تذكير غدًا`

ينشئ Scheduled Reminder بعد 1440 دقيقة ويغير الحالة إلى متابعة.

#### `⏰ بعد أسبوع`

ينشئ تذكير متابعة بعد 10080 دقيقة.

## `📝 إدارة الرسائل/القوالب`

يعرض القوالب النصية المحفوظة.

### `➕ إضافة رسالة جديدة`

1. أرسل اسم القالب.
2. أرسل نص الرسالة حتى 3500 حرف.
3. يحفظها في SQLite.

### `🗑 حذف #رقم اسم`

لا يحذف السجل فعليًا، بل يعطل القالب (`enabled=0`) حتى لا يستخدم في حملات جديدة.

## `👥 إرسال إلى المجموعات`

يبدأ إعداد حملة إرسال للمجموعات الموجودة فعليًا في حساب WhatsApp المختار.

## `👤 إرسال إلى الدردشات الحالية`

يبدأ حملة للمحادثات الشخصية الموجودة في السجل المحلي للحساب.

بعد اختيار الحساب تظهر الخيارات:

### `✅ كل العدد`

يختار كل الأهداف المتاحة.

### `🔢 تحديد عدد`

تكتب عددًا من بداية القائمة.

### `📋 عرض/اختيار المستلمين`

يفتح قائمة صفحات من 8 أهداف، ويظهر الاسم/الرقم عند توفرهما.

#### زر الهدف `⬜ الاسم` / `✅ الاسم`

يضيف أو يزيل المستلم من التحديد اليدوي.

#### `◀️` و`▶️`

تنقل بين صفحات المستلمين.

#### `1/5` ونحوه

مؤشر صفحة فقط، لا ينفذ تغييرًا.

#### `✅ اعتماد المحدد (عدد)`

يثبت قائمة الأهداف المختارة ويذهب لاختيار قالب الرسالة.

### اختيار القالب `📝 اسم القالب`

يحدد الرسالة التي سترسل.

### عدد الرسائل لكل هدف

- `1`
- `2`
- `3`
- `5`
- `🔢 عدد آخر`

العدد الأعلى يخضع لسقف `BROADCAST_MAX_MESSAGES_PER_TARGET`.

بعدها يطلب:

1. الفاصل بين الأهداف بالثواني.
2. حجم الدفعة.
3. الراحة بين الدفعات بالدقائق.
4. عدد دورات الإرسال.
5. الفاصل بين الدورات إذا كانت أكثر من دورة.

### عدد الدورات

- `مرة واحدة فقط`
- `دورتان`
- `3 دورات`
- `5 دورات`
- `🔢 عدد آخر`

### `✅ إنشاء وبدء الحملة`

بعد شاشة المراجعة ينشئ Campaign وJob ويبدأ التنفيذ في الخلفية.

### `▶️ استمرار الإرسال في بقية الأهداف`

إذا بقيت أهداف لم تنفذ، ينشئ Job جديدًا لاستكمال نفس الحملة دون تكرار الأهداف المكتملة في نفس الدورة.

## `🗂️ الدليل: دردشات / مجموعات / قنوات`

يعرض ملخصًا لكل حساب:

- عدد الدردشات الشخصية المحلية.
- عدد المجموعات من Provider.
- عدد القنوات المتزامنة محليًا.

## `📊 حملات الإرسال`

يعرض آخر 20 حملة وحالتها والدورة الحالية وعدد الأهداف المرسلة/الإجمالي.

إذا بقيت أهداف يظهر زر `▶️ استمرار الحملة #رقم`.

## `🚫 قائمة عدم الإرسال`

قائمة استبعاد دائمة نسبيًا داخل البوت للأهداف التي لا تريد دخولها الحملات.

### `➕ إضافة JID إلى عدم الإرسال`

ألصق JID مجموعة أو دردشة. الحملات تستبعد الهدف المدرج في هذه القائمة.

---

# 11) المهام والتقارير

## `📋 مهام التشغيل`

يعرض آخر 20 Job غير مخفي.

أنواع المهام المعروضة:

- تجميع.
- فحص.
- انضمام.
- إرسال رسائل.
- استيراد تاريخ Telegram.

### `📋 تقرير #رقم`

يفتح تفاصيل المهمة حسب نوعها، بما في ذلك Progress ونتائج الحسابات أو Audit أو Campaign.

### `🗑 حذف`

يخفي المهمة من القائمة (`hidden`) ولا يحذف البيانات التشغيلية المرتبطة بالضرورة.

### `⏸ إيقاف`

يرسل طلب Pause. تتوقف المهمة عند أقرب نقطة آمنة وتحفظ تقدمها عندما يدعم النوع ذلك.

### `⛔ إلغاء`

يرسل طلب Cancel للمهمة النشطة.

### `▶️ استكمال`

ينشئ Job جديدًا للاستكمال حسب النوع:

- Collection: يكمل من الجديد فقط.
- Audit: يستخدم Audit ID المحفوظ.
- Join: يعيد تشغيل نفس Payload على المتبقي/الحالات المتاحة.
- Broadcast: يستكمل نفس Campaign.
- Telegram History: يستخدم Source ID وCursor المحفوظ.

### `🔄 تحديث`

يعيد تحميل قائمة المهام وحالاتها.

## `⏰ المهام المجدولة`

يعرض آخر 30 Scheduled Task.

### `➕ مهمة مجدولة`

يسمح بجدولة أربع وظائف إدارية:

#### `⏰ تذكير`

يطلب نص التذكير ثم وقت التنفيذ.

#### `💾 نسخة آمنة`

ينشئ ويرسل Safe Backup في الموعد.

#### `🩺 صحة الحسابات`

ينفذ Account Health ويرسل النتيجة.

#### `🧰 تشخيص`

ينشئ ويرسل ملف Diagnostics.

### أوقات الجدولة

- `بعد 5 دقائق`
- `بعد 30 دقيقة`
- `بعد ساعة`
- `بعد 24 ساعة`
- `يوميًا بدءًا بعد 24 ساعة`

### `⛔ إلغاء #رقم`

يلغي Scheduled Task إذا كانت حالتها Scheduled أو Failed.

### `🔄 تحديث`

يعيد تحميل قائمة المهام المجدولة.

## `🩺 صحة الحسابات`

فحص مباشر لجميع الحسابات المفعلة.

## `📊 تقرير الحسابات`

التقرير الشامل للمالك.

## `📨 حملات الإرسال`

اختصار مباشر لسجل حملات الإرسال.

---

# 12) المراقبة المباشرة

## `➕ إضافة مراقبة`

يطلب JID لمجموعة WhatsApp. يأخذ أول حساب متصل متاح ضمن الصلاحيات ويبدأ Cursor من آخر رسالة محلية موجودة، ولذلك المراقبة تبدأ **من الرسائل الجديدة فقط**.

## `⏸ إيقاف #رقم` / `▶️ تشغيل #رقم`

يعطل أو يفعل المراقبة بدون حذفها.

## `🗑 حذف #رقم`

يحذف تعريف المراقبة.

وظيفة Watch Worker هي التقاط روابط WhatsApp الجديدة من الرسائل بعد Cursor المراقبة وإدخالها إلى نظام الروابط.

---

# 13) الإدارة والنظام

## `👥 إدارة المشرفين`

يظهر للمالك الحقيقي فقط.

### `➕ إضافة مشرف عادي`

أرسل Telegram User ID. يحصل على واجهة محدودة للحسابات والمنتهية والمهمشة.

### `⭐ إضافة مشرف رئيسي`

أرسل User ID. يحصل على وظائف البوت تقريبًا كاملة عدا إدارة المشرفين.

### `➖ حذف/تعطيل مشرف`

يعطل المشرف المحدد ويزيله من صلاحية Principal إن كان كذلك.

## `💾 نسخة آمنة`

ينشئ ZIP يمكن إرساله عبر Telegram يحتوي:

- نسخة SQLite.
- بعض ملفات الوثائق.

ويستبعد عمدًا:

- `.env`.
- جلسات WhatsApp.
- جلسة Telethon.

ويحتفظ بعدد نسخ حسب `BACKUP_KEEP_COUNT`.

## `🧰 التشخيص`

ينشئ ملف Diagnostics يحتوي:

- Python version.
- Node version.
- npm version.
- النظام.
- SQLite integrity.
- عدد الجداول وحجم القاعدة.
- مساحة القرص.
- مسار DB.
- Provider URL.

ولا يدرج Tokens أو Sessions أو API Hash.

## `⚠️ مركز الأخطاء`

يعرض آخر 20 System Error مسجل.

### `✅ إغلاق الخطأ #رقم`

يعلم الخطأ على أنه Resolved ولا يحذف سجله.

## `🧾 سجل الإدارة`

يعرض آخر 30 Admin Event مثل:

- Health Check.
- إنشاء Backup.
- تغيير حالة محادثة.
- إضافة Tag.
- إغلاق خطأ.
- تشغيل Scheduled Task.

## `🗄 فحص قاعدة البيانات`

ينفذ `PRAGMA integrity_check` ويعرض:

- نتيجة Integrity.
- عدد الجداول.
- الحجم المنطقي للقاعدة.

## `📦 نسخة محلية كاملة`

ينشئ Disaster-Recovery ZIP محليًا، وقد يحتوي:

- قاعدة البيانات.
- جلسات WhatsApp المحلية.
- جلسة Telethon.
- كود المشروع ووثائقه.

ولا يرسلها عبر Telegram لأن الجلسات حساسة. يظهر فقط مسار الملف المحلي.

## `⚠️ إعادة الضبط`

يفتح نوعي Reset.

### `حذف الروابط والمهام فقط`

Soft Reset بعد أخذ نسخة DB أولًا، مع الحفاظ على إعدادات أساسية مثل الحسابات/المشرفين بحسب منطق `factory_reset`.

### `إعادة ضبط كاملة`

للمالك فقط. ينفذ Reset أوسع بعد Backup.

---

# 14) وظائف تعمل في الخلفية بدون زر مباشر

## مزامنة WhatsApp `worker_forever`

تعمل عند تشغيل البوت وتقوم بمزامنة الرسائل والحفاظ على Cursors وفق إعدادات `WA_SYNC_INTERVAL` و`WA_SYNC_BATCH`.

## Scheduled Task Worker

يفحص SQLite دوريًا كل `SCHEDULER_INTERVAL_SECONDS` ويشغل المهمة المجدولة المستحقة.

## استعادة المهام المتقطعة

عند تشغيل البوت يستدعي `recover_interrupted_jobs()` حتى لا تبقى المهام القديمة عالقة كأنها Running بعد إعادة تشغيل غير متوقعة.

## منع التكرار العالمي

`links.normalized_url` فريد عالميًا، بينما `link_sections` يسمح لنفس الرابط أن ينتمي لأكثر من قسم بدون نسخ الرابط نفسه عدة مرات.

## Cursors

توجد مؤشرات تقدم للمزامنة والتجميع وTelegram History وبعض العمليات، حتى يمكن قراءة الجديد أو استكمال العمليات بدل إعادة كل شيء من الصفر.

---

# 15) أقسام الروابط

## Important

الروابط المهمة والقابلة عادةً للانتقال إلى Join Queue.

## Students

روابط الطلبة.

## Channels

قسم القنوات/المصادر؛ يمكن أن يحتوي روابط متنوعة، لكن Join Worker ينفذ فقط على روابط مجموعات WhatsApp.

## Expired

سجل استبعاد للروابط المنتهية.

## Ignored

سجل استبعاد للروابط التي لا تريد استخدامها.

---

# 16) أوامر Termux / Ubuntu

## تشغيل

```bash
./botctl.sh start
```

يفحص Python وNode أولًا ثم يشغل Provider والبوت.

## إيقاف

```bash
./botctl.sh stop
```

## إعادة تشغيل

```bash
./botctl.sh restart
```

## الحالة

```bash
./botctl.sh status
```

## السجلات الحية

```bash
./botctl.sh logs
```

## فحص الإصدارات

```bash
./botctl.sh versioncheck
```

المعتمد:

- Python 3.12 أو 3.13 أو 3.14.
- Node 22 LTS.

## الفحص الكامل

```bash
./botctl.sh selfcheck
```

ينفذ:

- Runtime version check.
- Python compileall.
- Node syntax check.
- Import check لـ Baileys من مجلد `provider`.
- اختبار Message Text Parser.
- اختبارات V2.0 حتى V2.6.

## Safe Backup

```bash
./botctl.sh backup
```

## Local Full Backup

```bash
./botctl.sh fullbackup
```

## Diagnostics

```bash
./botctl.sh diag
```

## DB Check

```bash
./botctl.sh dbcheck
```

## Restore DB

أوقف البوت أولًا:

```bash
./botctl.sh stop
./botctl.sh restore-db /path/to/whatsapp_ops.db
./botctl.sh start
```

قبل الاستبدال يفحص SQLite وينشئ نسخة `pre_restore` من القاعدة الحالية.

---

# 17) أهم إعدادات `.env`

## `BOT_TOKEN`

توكن بوت Telegram.

## `OWNER_ID`

Telegram User ID للمالك.

## `INSTANCE_NAME`

الاسم الظاهر أعلى واجهة البوت.

## `DB_PATH`

مسار SQLite.

## `WA_PROVIDER_URL`

العنوان المحلي لـ Node Provider، افتراضيًا `http://127.0.0.1:8765`.

## `WA_PROVIDER_TOKEN`

سر داخلي بين Python وProvider. يجب تغييره إلى قيمة عشوائية طويلة.

## `WA_SYNC_INTERVAL`

الفاصل بين دورات Sync المحلية.

## `WA_SYNC_BATCH`

حجم دفعة رسائل Sync.

## `WEB_AUDIT_WORKERS`

عدد مهام فحص الويب المتوازية ضمن السقف المحدد في config.

## `JOIN_ITEM_DELAY_SECONDS`

القيمة الافتراضية لفاصل محاولات الانضمام عند استخدامها من الخدمات؛ الواجهة تسمح بتحديد قيم لكل مهمة.

## `BROADCAST_MAX_MESSAGES_PER_TARGET`

السقف البرمجي لعدد الرسائل داخل كل هدف في الحملة.

## `BROADCAST_MAX_REPEAT_CYCLES`

الحد الأعلى لدورات التكرار.

## `TELEGRAM_API_ID / TELEGRAM_API_HASH`

اختياريان فقط لاستيراد تاريخ Telegram القديم عبر Telethon.

## `SCHEDULER_INTERVAL_SECONDS`

كل كم ثانية يفحص Scheduler المهام المستحقة.

## `HEALTH_CHECK_TIMEOUT_SECONDS`

مهلة فحص كل حساب WhatsApp.

## `BACKUP_KEEP_COUNT`

عدد Safe Backups التي يحتفظ بها تلقائيًا.

---

# 18) مثال استخدام كامل: جمع ثم فحص ثم انضمام

1. اربط حساب WhatsApp من `📱 الحسابات`.
2. افتح `🔗 الروابط والتجميع`.
3. اختر `⚡ تجميع من WhatsApp`.
4. حدد نوع الروابط والمصدر وطريقة القراءة والفترة.
5. تابع Job من `📋 مهام التشغيل`.
6. افتح `🔍 الفحص والانضمام` ثم `🧪 فحص روابط WhatsApp`.
7. افحص قاعدة البيانات.
8. بعد التقرير اضغط `➕ إضافة المجموعات الصالحة لطابور الانضمام`.
9. افتح `➕ إدارة طابور الانضمام`.
10. اختر القسم ثم الحساب ثم العدد والفاصل والدفعات.
11. تابع المهمة من مركز Jobs.

---

# 19) مثال استخدام كامل: متابعة محادثة

1. افتح `📨 إدارة الرسائل`.
2. اضغط `📥 صندوق المتابعة`.
3. افتح المحادثة المطلوبة.
4. ضع حالتها `⭐ مهم` أو `🟡 متابعة`.
5. أضف `📝 ملاحظة` و`🏷 Tag` عند الحاجة.
6. اضغط `⏰ تذكير غدًا` أو `⏰ بعد أسبوع`.
7. ستظهر المهمة في `⏰ المهام المجدولة`.

---

# 20) مثال استخدام كامل: حملة رسائل

1. أنشئ قالبًا من `📝 إدارة الرسائل/القوالب`.
2. اختر `👥 إرسال إلى المجموعات` أو `👤 إرسال إلى الدردشات الحالية`.
3. اختر الحساب.
4. اختر كل الأهداف أو عددًا أو مستلمين يدويًا.
5. اختر القالب.
6. اختر عدد الرسائل لكل هدف.
7. حدد الفاصل وحجم الدفعة والراحة.
8. حدد عدد الدورات والفاصل بينها إن لزم.
9. راجع الملخص.
10. اضغط `✅ إنشاء وبدء الحملة`.
11. تابعها من `📊 حملات الإرسال` أو `📋 مهام التشغيل`.
12. إذا توقفت وبقيت أهداف استخدم `▶️ استمرار الحملة`.

---

# 21) ما الذي لا يفعله البوت

- لا يتجاوز Rate Limits أو Try Later.
- لا يضمن بقاء جلسة WhatsApp إذا غيرت WhatsApp بروتوكول الويب مستقبلًا.
- لا يستطيع استرجاع رسائل لم يرسلها WhatsApp إلى جلسة Multi-Device المحلية.
- لا يستورد تاريخ Telegram القديم بدون Telethon Session مخولة.
- لا يرسل Safe Backup وفيه أسرار؛ النسخة التي تحتوي جلسات حساسة تبقى محلية عمدًا.

---

# 22) ملاحظة عن الملفات وحجم الإصدار

النسخة المدمجة تعتمد **ملفات المصدر** لا ملفات cache. ملفات `__pycache__` و`.pyc` من V2.5 الأصلية ليست وظائف إضافية؛ هي ناتج ترجمة تلقائي مرتبط بإصدار Python، لذلك استبعادها يجعل ZIP أصغر وأكثر نظافة ويمنع تعارض cache قديم مع Python جديد.
# تحديث واجهة V2.7

- داخل «الروابط والتجميع» يوجد زر «استيراد روابط من ملف TXT» مع اختيار القسم.
- داخل «مصادر Telegram» يوجد زر «جلسات Telegram واستخراج جلسة» لإنشاء جلسة Telethon من البوت.
- عند إضافة مصدر Telegram تختار القسم ثم جلسة المستخدم أو وصول البوت المباشر.
- كل تقرير مهمة يحتوي زر «تحميل التقرير الكامل TXT».
- مزامنة مصادر Telegram التلقائية تظهر كمهمة مستقلة لكل مصدر.

