# تحقق V2.8

- نجح تجميع Python واستيراد `app.bot` باستخدام إعداد اختبار.
- نجحت اختبارات V2.0–V2.7 بعد تحديث توقعات الصلاحيات والإصدار مع بقاء الوظائف القديمة.
- نجح اختبار ترحيل قاعدة V2.7.
- نجح اختبار V2.8 الفعلي للصلاحيات، القاطع اليومي، التنبيهات، ضغط الأرشيف واستعادته.
- نجح اختبار عامل الانضمام الذي يثبت أن رابطين مختلفين للمجموعة نفسها ينتجان محاولة مزود واحدة وحالة `duplicate_group` للثانية.
- نجح `node --check` واختبار استخراج نص الرسائل.
- الحزمة لا تشمل `.env` أو قاعدة مستخدم أو جلسات WhatsApp/Telethon أو `node_modules` أو ملفات cache.
